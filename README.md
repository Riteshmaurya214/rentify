# Rentify

Run `npm install` then `npm run seed` to create demo data, then `npm run dev`.


OLX-style changes added: single 'user' role, video visible to guests, rent requests require login, notifications added. Run `npm run seed` and then `npm run dev`.


Final features: search (title/description/location), filters, delete product, notification bell with mark-read, dark/light toggle. Seeds include Ritesh/Shivam/Vihaan.
